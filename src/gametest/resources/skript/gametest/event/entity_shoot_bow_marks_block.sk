on skeleton shoot bow:
    if consumed item is set:
        set test name of item consumed item to "shot arrow"
        if name of consumed item is "shot arrow":
            set test block at 4 1 0 to emerald_block
