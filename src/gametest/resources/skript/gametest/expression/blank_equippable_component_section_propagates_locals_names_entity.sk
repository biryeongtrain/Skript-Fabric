on use entity:
    set {_state} to "outside"
    set {_component} to a blank equippable component:
        set {_state} to "inside"
    {_state} is "inside"
    set test name of entity event-entity to "blank equippable locals"
