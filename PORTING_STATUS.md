# Porting Status

Canonical document moved to [docs/porting/PORTING_STATUS.md](docs/porting/PORTING_STATUS.md).

Current headline:

- Stage 5 event backend closure: `22 / 22`
- Stage 8 package-local audit: `23 / 214`
- latest verified runtime baseline: `216 / 216` scheduled Fabric GameTests and `./gradlew build --rerun-tasks` passed on 2026-03-08
- new immediate priority: missing upstream user-visible syntax import first, with `ch/njol/skript` closure workstream (`128 / 1189` local versus upstream snapshot `e6ec744`) as the enabling track
- Codex parallel-session runbook: `docs/porting/CODEX_PARALLEL_WORKFLOW.md`
